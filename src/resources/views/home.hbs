<h1>Chào mừng đến với bookstore với docker</h1>
<div class="row">
    {{#each books}}
    <div class="col-md-4">
        <h3>{{this.title}}</h3>
        <p>tac gia: {{this.author}}</p>
        <p>{{this.description}}</p>
    </div>
    {{/each}}
</div>