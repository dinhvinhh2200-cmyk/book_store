<div class="container mt-5">
    <div class="row">
        <div class="col-md-4">
            <img src="/img/{{book.image}}" alt="{{book.title}}" class="img-fluid rounded">
        </div>

        <div class="col-md-8">
            <h1>{{book.title}}</h1>
            <p class="text-muted">Tac gia: <strong>{{book.author}}</strong></p>
            <hr>
            <h5>Mo ta:</h5>
            <p>{{book.description}}</p>
            
        </div>
    </div>
</div>